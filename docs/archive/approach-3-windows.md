# 早期 Windows `.exe` 方案（已移除）

此方案是專案早期的 Windows 語音輸入原型：Python 錄音、OpenAI Whisper API、系統匣圖示、全域熱鍵與文字貼上，並以 `build.bat` 打包成 `.exe`。

2026-07-22 盤點確認目前主力 VoiceKey（macOS）及其建置、測試、部署流程都不依賴此方案，因此移除原始碼目錄，只保留本摘要。完整原始碼仍可從 Git 歷史中的 `1f70b09`、`7142d3d`、`edcbc22` 等 commit 取回。
